(ns com.lambdaseq.stack.resources-router.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.resources-router.api :as resources-router]))

(deftest dummy-test
  (is (= 1 1)))
