(ns com.lambdaseq.stack.router-aggregator.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.router-aggregator.api :as router-aggregator]))

(deftest dummy-test
  (is (= 1 1)))
