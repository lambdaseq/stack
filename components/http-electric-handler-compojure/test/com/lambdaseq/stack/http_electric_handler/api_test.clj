(ns com.lambdaseq.stack.http-electric-handler.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.http-electric-handler.api :as http-electric-handler]))

(deftest dummy-test
  (is (= 1 1)))
