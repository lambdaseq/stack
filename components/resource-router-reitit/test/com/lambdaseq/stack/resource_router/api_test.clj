(ns com.lambdaseq.stack.resource-router.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.resource-router.api :as resource-router]))

(deftest dummy-test
  (is (= 1 1)))
