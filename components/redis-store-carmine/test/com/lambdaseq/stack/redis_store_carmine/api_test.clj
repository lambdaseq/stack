(ns com.lambdaseq.stack.redis-store-carmine.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.redis-store-carmine.api :as redis-store-carmine]))

(deftest dummy-test
  (is (= 1 1)))
