(ns com.lambdaseq.stack.persistence-xtdb.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.persistence-xtdb.api :as persistence-xtdb]))

(deftest dummy-test
  (is (= 1 1)))
