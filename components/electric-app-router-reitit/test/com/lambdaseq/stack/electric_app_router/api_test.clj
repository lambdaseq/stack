(ns com.lambdaseq.stack.electric-app-router.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.electric-app-router.api :as electric-app-router]))

(deftest dummy-test
  (is (= 1 1)))
