(ns com.lambdaseq.stack.html-renderer.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.html-renderer.api :as html-renderer]))

(deftest dummy-test
  (is (= 1 1)))
