(ns com.lambdaseq.stack.http-middleware.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.http-middleware.api :as http-middleware]))

(deftest dummy-test
  (is (= 1 1)))
