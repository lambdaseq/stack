(ns com.lambdaseq.stack.basic-http-router.api-test
  (:require [clojure.test :as test :refer :all]
            [com.lambdaseq.stack.basic-http-router.api :as basic-http-router]))

(deftest dummy-test
  (is (= 1 1)))
